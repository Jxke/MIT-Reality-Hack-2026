services:
  whisper-stt:
    build: ./whisper
    network_mode: host
    volumes:
      - ./whisper/models:/models:ro
    command: >
      /app/build/bin/whisper-server
      --host 0.0.0.0
      --port 8080
      --model /models/${WHISPER_MODEL:-ggml-base.en.bin}
      --threads 4
    restart: unless-stopped

  pipeline:
    build: ./pipeline
    network_mode: host
    depends_on:
      - whisper-stt
    environment:
      - WHISPER_URL=http://localhost:8080
      - ARDUINO_HOST=${ARDUINO_HOST:-0.0.0.0}
      - ARDUINO_PORT=${ARDUINO_PORT:-9000}
      - AR_HOST=0.0.0.0
      - AR_PORT=${AR_PORT:-9001}
      - POLL_INTERVAL=${POLL_INTERVAL:-1.0}
      - PULSE_SERVER=unix:/run/user/1000/pulse/native
    volumes:
      - /run/user/1000/pulse/native:/run/user/1000/pulse/native
    restart: unless-stopped
